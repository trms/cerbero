From a731dc813d59b8e069474b52befa22151bf653b8 Mon Sep 17 00:00:00 2001
From: Jonas Kvinge <jonas@jkvinge.net>
Date: Sat, 4 Apr 2026 02:03:51 +0200
Subject: [PATCH 15/18] openssl: Fix threading race conditions

Add mutex guards for SSL objects
---
 tls/openssl/gtlsconnection-openssl.c       | 85 ++++++++++++++++------
 tls/openssl/gtlsconnection-openssl.h       |  5 +-
 tls/openssl/gtlsserverconnection-openssl.c |  6 +-
 3 files changed, 73 insertions(+), 23 deletions(-)

diff --git a/tls/openssl/gtlsconnection-openssl.c b/tls/openssl/gtlsconnection-openssl.c
index 9f36785..83e9168 100644
--- a/tls/openssl/gtlsconnection-openssl.c
+++ b/tls/openssl/gtlsconnection-openssl.c
@@ -90,6 +90,7 @@ g_tls_connection_openssl_finalize (GObject *object)
 static GTlsConnectionBaseStatus
 end_openssl_io (GTlsConnectionOpenssl  *openssl,
                 GIOCondition            direction,
+                SSL                    *ssl,
                 int                     ret,
                 gboolean                blocking,
                 GError                **error,
@@ -100,12 +101,9 @@ end_openssl_io (GTlsConnectionOpenssl  *openssl,
   int err_code, err, err_lib, reason;
   GError *my_error = NULL;
   GTlsConnectionBaseStatus status;
-  SSL *ssl;
 
   priv = g_tls_connection_openssl_get_instance_private (openssl);
 
-  ssl = g_tls_connection_openssl_get_ssl (openssl);
-
   err_code = SSL_get_error (ssl, ret);
 
   status = g_tls_connection_base_pop_io (tls, direction, ret > 0, &my_error);
@@ -278,7 +276,7 @@ perform_openssl_io (GTlsConnectionOpenssl  *openssl,
 
   tls = G_TLS_CONNECTION_BASE (openssl);
   priv = g_tls_connection_openssl_get_instance_private (openssl);
-  ssl = g_tls_connection_openssl_get_ssl (openssl);
+  ssl = g_tls_connection_openssl_lock_ssl (openssl);
 
   if (timeout >= 0)
     deadline = g_get_monotonic_time () + timeout;
@@ -316,7 +314,7 @@ perform_openssl_io (GTlsConnectionOpenssl  *openssl,
             break;
         }
 
-      status = end_openssl_io (openssl, direction, ret, TRUE, error, err_prefix);
+      status = end_openssl_io (openssl, direction, ssl, ret, TRUE, error, err_prefix);
 
       if (status != G_TLS_CONNECTION_BASE_TRY_AGAIN)
         break;
@@ -363,6 +361,8 @@ perform_openssl_io (GTlsConnectionOpenssl  *openssl,
   if (out_ret)
     *out_ret = ret;
 
+  g_tls_connection_openssl_unlock (openssl);
+
   return status;
 }
 
@@ -446,7 +446,7 @@ g_tls_connection_openssl_prepare_handshake (GTlsConnectionBase  *tls,
   if (!advertised_protocols)
     return;
 
-  ssl = g_tls_connection_openssl_get_ssl (G_TLS_CONNECTION_OPENSSL (tls));
+  ssl = g_tls_connection_openssl_lock_ssl (G_TLS_CONNECTION_OPENSSL (tls));
 
   if (G_IS_TLS_SERVER_CONNECTION (tls))
     {
@@ -455,6 +455,7 @@ g_tls_connection_openssl_prepare_handshake (GTlsConnectionBase  *tls,
       g_tls_log_debug (tls, "Setting ALPN Callback on %p", ctx);
       SSL_CTX_set_alpn_select_cb (ctx, _openssl_alpn_select_cb, tls);
 
+      g_tls_connection_openssl_unlock (G_TLS_CONNECTION_OPENSSL (tls));
       return;
     }
 
@@ -481,6 +482,8 @@ g_tls_connection_openssl_prepare_handshake (GTlsConnectionBase  *tls,
         }
       g_byte_array_unref (protocols);
     }
+
+  g_tls_connection_openssl_unlock (G_TLS_CONNECTION_OPENSSL (tls));
 }
 
 static GTlsCertificateFlags
@@ -558,7 +561,7 @@ g_tls_connection_openssl_complete_handshake (GTlsConnectionBase   *tls,
   if (!handshake_succeeded)
     return;
 
-  ssl = g_tls_connection_openssl_get_ssl (G_TLS_CONNECTION_OPENSSL (tls));
+  ssl = g_tls_connection_openssl_lock_ssl (G_TLS_CONNECTION_OPENSSL (tls));
   session = SSL_get_session (ssl);
 
   SSL_get0_alpn_selected (ssl, &data, &len);
@@ -574,6 +577,8 @@ g_tls_connection_openssl_complete_handshake (GTlsConnectionBase   *tls,
   *protocol_version = session ? glib_protocol_version_from_openssl (SSL_SESSION_get_protocol_version (session))
                               : G_TLS_PROTOCOL_VERSION_UNKNOWN;
   *ciphersuite_name = g_strdup (SSL_get_cipher_name (ssl));
+
+  g_tls_connection_openssl_unlock (G_TLS_CONNECTION_OPENSSL (tls));
 }
 
 static GTlsCertificate *
@@ -584,21 +589,28 @@ g_tls_connection_openssl_retrieve_peer_certificate (GTlsConnectionBase *tls)
   GTlsCertificateOpenssl *chain;
   SSL *ssl;
 
-  ssl = g_tls_connection_openssl_get_ssl (G_TLS_CONNECTION_OPENSSL (tls));
+  ssl = g_tls_connection_openssl_lock_ssl (G_TLS_CONNECTION_OPENSSL (tls));
 
   peer = SSL_get_peer_certificate (ssl);
   if (!peer)
-    return NULL;
+    {
+      g_tls_connection_openssl_unlock (G_TLS_CONNECTION_OPENSSL (tls));
+      return NULL;
+    }
 
   certs = SSL_get_peer_cert_chain (ssl);
   if (!certs)
     {
       X509_free (peer);
+      g_tls_connection_openssl_unlock (G_TLS_CONNECTION_OPENSSL (tls));
       return NULL;
     }
 
   chain = g_tls_certificate_openssl_build_chain (peer, certs);
   X509_free (peer);
+
+  g_tls_connection_openssl_unlock (G_TLS_CONNECTION_OPENSSL (tls));
+
   if (!chain)
     return NULL;
 
@@ -610,22 +622,26 @@ openssl_get_binding_tls_unique (GTlsConnectionOpenssl  *tls,
                                 GByteArray             *data,
                                 GError                **error)
 {
-  SSL *ssl = g_tls_connection_openssl_get_ssl (tls);
+  SSL *ssl = g_tls_connection_openssl_lock_ssl (tls);
   gboolean is_client = G_IS_TLS_CLIENT_CONNECTION (tls);
-  gboolean resumed = SSL_session_reused (ssl);
+  gboolean resumed;
   size_t len = 64;
 
   if (SSL_version (ssl) >= TLS1_3_VERSION)
     {
+      g_tls_connection_openssl_unlock (tls);
       g_set_error_literal (error, G_TLS_CHANNEL_BINDING_ERROR, G_TLS_CHANNEL_BINDING_ERROR_GENERAL_ERROR,
                            _("The request is invalid."));
       return FALSE;
     }
 
   /* This is a drill */
-  if (!data)
+  if (!data) {
+    g_tls_connection_openssl_unlock (tls);
     return TRUE;
+  }
 
+  resumed = SSL_session_reused (ssl);
   do {
     g_byte_array_set_size (data, len);
     if ((resumed && is_client) || (!resumed && !is_client))
@@ -633,6 +649,7 @@ openssl_get_binding_tls_unique (GTlsConnectionOpenssl  *tls,
     else
       len = SSL_get_finished (ssl, data->data, data->len);
   } while (len > data->len);
+  g_tls_connection_openssl_unlock (tls);
 
   if (len > 0)
     {
@@ -649,7 +666,7 @@ openssl_get_binding_tls_server_end_point (GTlsConnectionOpenssl  *tls,
                                           GByteArray             *data,
                                           GError                **error)
 {
-  SSL *ssl = g_tls_connection_openssl_get_ssl (tls);
+  SSL *ssl = g_tls_connection_openssl_lock_ssl (tls);
   gboolean is_client = G_IS_TLS_CLIENT_CONNECTION (tls);
   int algo_nid;
   const EVP_MD *algo = NULL;
@@ -659,6 +676,7 @@ openssl_get_binding_tls_server_end_point (GTlsConnectionOpenssl  *tls,
     crt = SSL_get_peer_certificate (ssl);
   else
     crt = SSL_get_certificate (ssl);
+  g_tls_connection_openssl_unlock (tls);
 
   if (!crt)
     {
@@ -721,13 +739,16 @@ openssl_get_binding_tls_exporter (GTlsConnectionOpenssl  *tls,
                                   GByteArray             *data,
                                   GError                **error)
 {
-  SSL *ssl = g_tls_connection_openssl_get_ssl (tls);
+  SSL *ssl = g_tls_connection_openssl_lock_ssl (tls);
   size_t  ctx_len = 0;
   guint8 *context = (guint8 *)"";
   int ret;
 
   if (!data)
-    return TRUE;
+    {
+      g_tls_connection_openssl_unlock (tls);
+      return TRUE;
+    }
 
   g_byte_array_set_size (data, 32);
   ret = SSL_export_keying_material (ssl,
@@ -735,6 +756,7 @@ openssl_get_binding_tls_exporter (GTlsConnectionOpenssl  *tls,
                                     RFC5705_LABEL_DATA, RFC5705_LABEL_LEN,
                                     context, ctx_len,
                                     1 /* use context */);
+  g_tls_connection_openssl_unlock (tls);
 
   if (ret > 0)
     return TRUE;
@@ -831,8 +853,6 @@ g_tls_connection_openssl_push_io (GTlsConnectionBase *tls,
       g_clear_error (error);
       g_tls_bio_set_write_error (priv->bio, error);
     }
-
-  g_mutex_lock (&priv->ssl_mutex);
 }
 
 static GTlsConnectionBaseStatus
@@ -846,8 +866,6 @@ g_tls_connection_openssl_pop_io (GTlsConnectionBase  *tls,
 
   priv = g_tls_connection_openssl_get_instance_private (openssl);
 
-  g_mutex_unlock (&priv->ssl_mutex);
-
   if (direction & G_IO_IN)
     g_tls_bio_set_read_cancellable (priv->bio, NULL);
 
@@ -1074,7 +1092,7 @@ g_tls_connection_openssl_initable_init (GInitable     *initable,
 
   priv = g_tls_connection_openssl_get_instance_private (openssl);
 
-  ssl = g_tls_connection_openssl_get_ssl (openssl);
+  ssl = g_tls_connection_openssl_lock_ssl (openssl);
   g_assert (ssl);
 
   SSL_set_ex_data (ssl, data_index, openssl);
@@ -1089,6 +1107,8 @@ g_tls_connection_openssl_initable_init (GInitable     *initable,
   g_clear_object (&base_io_stream);
   g_clear_object (&base_socket);
 
+  g_tls_connection_openssl_unlock (openssl);
+
   return TRUE;
 }
 
@@ -1109,10 +1129,11 @@ g_tls_connection_openssl_init (GTlsConnectionOpenssl *openssl)
 }
 
 SSL *
-g_tls_connection_openssl_get_ssl (GTlsConnectionOpenssl *openssl)
+g_tls_connection_openssl_lock_ssl (GTlsConnectionOpenssl *openssl)
 {
   g_return_val_if_fail (G_IS_TLS_CONNECTION_OPENSSL (openssl), NULL);
 
+  g_tls_connection_openssl_lock (openssl);
   return G_TLS_CONNECTION_OPENSSL_GET_CLASS (openssl)->get_ssl (openssl);
 }
 
@@ -1123,3 +1144,25 @@ g_tls_connection_openssl_get_connection_from_ssl (SSL *ssl)
 
   return SSL_get_ex_data (ssl, data_index);
 }
+
+void
+g_tls_connection_openssl_lock (GTlsConnectionOpenssl *openssl)
+{
+  GTlsConnectionOpensslPrivate *priv;
+
+  g_return_if_fail (G_IS_TLS_CONNECTION_OPENSSL (openssl));
+
+  priv = g_tls_connection_openssl_get_instance_private (openssl);
+  g_mutex_lock (&priv->ssl_mutex);
+}
+
+void
+g_tls_connection_openssl_unlock (GTlsConnectionOpenssl *openssl)
+{
+  GTlsConnectionOpensslPrivate *priv;
+
+  g_return_if_fail (G_IS_TLS_CONNECTION_OPENSSL (openssl));
+
+  priv = g_tls_connection_openssl_get_instance_private (openssl);
+  g_mutex_unlock (&priv->ssl_mutex);
+}
diff --git a/tls/openssl/gtlsconnection-openssl.h b/tls/openssl/gtlsconnection-openssl.h
index 1682eed..031758d 100644
--- a/tls/openssl/gtlsconnection-openssl.h
+++ b/tls/openssl/gtlsconnection-openssl.h
@@ -43,10 +43,13 @@ struct _GTlsConnectionOpensslClass
   SSL *(*get_ssl) (GTlsConnectionOpenssl *connection);
 };
 
-SSL *g_tls_connection_openssl_get_ssl (GTlsConnectionOpenssl *connection);
+SSL *g_tls_connection_openssl_lock_ssl (GTlsConnectionOpenssl *connection);
 
 GTlsConnectionOpenssl *g_tls_connection_openssl_get_connection_from_ssl (SSL *ssl);
 
 GTlsProtocolVersion glib_protocol_version_from_openssl (int protocol_version);
 
+void g_tls_connection_openssl_lock   (GTlsConnectionOpenssl *openssl);
+void g_tls_connection_openssl_unlock (GTlsConnectionOpenssl *openssl);
+
 G_END_DECLS
diff --git a/tls/openssl/gtlsserverconnection-openssl.c b/tls/openssl/gtlsserverconnection-openssl.c
index bce326f..56bfb68 100644
--- a/tls/openssl/gtlsserverconnection-openssl.c
+++ b/tls/openssl/gtlsserverconnection-openssl.c
@@ -237,7 +237,11 @@ on_certificate_changed (GObject    *object,
   cert = g_tls_connection_get_certificate (G_TLS_CONNECTION (object));
 
   if (ssl && cert)
-    ssl_set_certificate (ssl, cert, NULL);
+    {
+      g_tls_connection_openssl_lock (G_TLS_CONNECTION_OPENSSL (object));
+      ssl_set_certificate (ssl, cert, NULL);
+      g_tls_connection_openssl_unlock (G_TLS_CONNECTION_OPENSSL (object));
+    }
 }
 
 static void
-- 
2.20.1.windows.1

