From a0b82117a79e6c2ae568aed81cb58fb51e567277 Mon Sep 17 00:00:00 2001
From: Ray Tiley <ray.tiley@trms.com>
Date: Wed, 19 May 2021 17:30:45 -0400
Subject: [PATCH 5/6] add duplex mode to decklinksink

---
 sys/decklink/gstdecklink.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/sys/decklink/gstdecklink.cpp b/sys/decklink/gstdecklink.cpp
index 5e1e441ce..803320556 100644
--- a/sys/decklink/gstdecklink.cpp
+++ b/sys/decklink/gstdecklink.cpp
@@ -1722,7 +1722,7 @@ gst_decklink_acquire_nth_output (gint n, GstElement * sink, gboolean is_audio)
   if (!is_audio) {
     GstDecklinkVideoSink *videosink = (GstDecklinkVideoSink *) (sink);
     if (gst_decklink_configure_duplex_mode (device,
-            videosink->duplex_mode) == DUPLEX_MODE_SET_FAILURE) {
+            bmdDuplexModeHalf) == DUPLEX_MODE_SET_FAILURE) {
       return NULL;
     }
   }
-- 
2.20.1.windows.1

